const register = location.pathname === "/register";
document.getElementById("authForm").addEventListener("submit", async event => { event.preventDefault(); const error = document.getElementById("error"); error.textContent=""; try { const response = await fetch(`/auth/${register ? "register" : "login"}`, {method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:email.value.trim(),password:password.value})}); const body=await response.json(); if(!response.ok) throw new Error(body.error || "Unable to continue"); if(register){ location.assign("/login"); return; } localStorage.setItem("producthub_token",body.token); localStorage.setItem("producthub_user",JSON.stringify(body.user)); location.assign("/"); } catch(err){ error.textContent=err.message; } });
